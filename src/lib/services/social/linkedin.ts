/**
 * LinkedIn Platform Adapter — Stub (not yet implemented).
 */

import { StubAdapter } from "./stub-adapter";

export class LinkedInAdapter extends StubAdapter {
  platform = "linkedin" as const;
}
